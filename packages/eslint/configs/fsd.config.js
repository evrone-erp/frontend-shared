import fsdRules from '../lint/fsd-rules.js';

/**
 * Дополнительный конфиг с правилами Feature-Sliced Design.
 * @see README.md для информации о подключении
 */
const fsdConfig = [
  {
    rules: fsdRules,
  },
];

export default fsdConfig;
